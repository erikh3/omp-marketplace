{
  "name": "hai-jwt-retry",
  "version": "0.2.0",
  "description": "Auto-retries transient HAI Proxy authentication and encrypted-reasoning failures.",
  "private": true,
  "omp": {
    "extensions": [
      "./src/index.ts"
    ],
    "settings": {
      "enabled": { "type": "boolean", "default": true, "description": "Enable automatic retries for supported HAI Proxy failures" },
      "proxyHost": { "type": "string", "default": "localhost", "description": "HAI Proxy host (must match hai-proxy-companion if installed)" },
      "proxyPort": { "type": "number", "default": 6655, "description": "HAI Proxy port" }
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@oh-my-pi/pi-coding-agent": "^18.1.14",
    "@oh-my-pi/pi-ai": "^18.1.14",
    "bun-types": "^1.4.2",
    "typescript": "^7.0.2"
  }
}
