{
  "name": "session-autoname",
  "version": "0.0.1",
  "description": "Auto-name the session from its logs via the smol model when /name or /rename is issued without a title.",
  "private": true,
  "omp": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@oh-my-pi/pi-coding-agent": "^17.0.0",
    "bun-types": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
