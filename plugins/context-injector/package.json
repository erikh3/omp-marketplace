{
  "name": "context-injector",
  "version": "0.1.0",
  "description": "Inject skills and arbitrary file contents into the LLM context at agent-start time. Configurable via context-injector.yml — supports skill:// URIs, globs, per-entry once/display flags, and both user and project scopes.",
  "private": true,
  "omp": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "test": "bun test --isolate",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@oh-my-pi/pi-coding-agent": "^17.0.0",
    "bun-types": "^1.4.0",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
