{
  "name": "github-comment-guard",
  "version": "0.1.0",
  "description": "Blocks pull request reviews and human-authored GitHub comment replies. Gate is on by default; inspect or change it with /github-comment-gate.",
  "private": true,
  "omp": {
    "extensions": [
      "./src/index.ts"
    ],
    "settings": {
      "persistGateState": {
        "type": "boolean",
        "default": true,
        "description": "Remember gate state across OMP sessions"
      }
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@oh-my-pi/pi-coding-agent": "^18.1.14",
    "bun-types": "^1.4.2",
    "typescript": "^7.0.2"
  }
}
